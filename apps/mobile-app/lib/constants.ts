// Navigation theme colors mapped to the closest values from global.css for light and dark mode.
// These are not exhaustive, but cover the major keys for navigation UI.
export const NAV_THEME = {
  light: {
    background: '#fcfff5', // --background
    border: '#d4e9d4',     // --border
    card: '#fcfff5',       // --card
    notification: '#d85252', // --destructive
    primary: '#d85252',    // --primary
    text: '#003300',       // --foreground
  },
  dark: {
    background: '#0a140f', // --background (dark)
    border: '#2a3b32',     // --border (dark)
    card: '#102118',       // --card (dark)
    notification: '#8b0000', // --destructive (dark)
    primary: '#d85252',    // --primary (dark)
    text: '#e0e0e0',       // --foreground (dark)
  },
};
